---
name: check
description: Run validation (format, lint, tsc, test) via sideclaw MCP tool
---

# Check — via sideclaw MCP

Call `mcp__sideclaw__check` with `cwd` set to the current working directory.
