---
description: Markdown table formatting rules
---

# Formatting Rules

**Tables:**
- Markdown tables: use minimum separator (`|-|-|`). Never pad with repeated hyphens.
- NEVER use box-drawing / ASCII-art tables (`+`, `-`, `|` grids, or Unicode box chars like `┌┬─│└┘├┤┼`).
