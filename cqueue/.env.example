PERSONAL_REPOS_PATH=/Users/yourname/SourceRoot
WORK_REPOS_PATH=/Users/yourname/IuRoot
CQUEUE_PORT=7705
UID=501
GID=20
# GitHub token for PR status, CI checks, workflow runs, and release trigger
# Option A (recommended): reuse local gh CLI session
#   GITHUB_TOKEN=$(gh auth token)
# Option B: create a classic PAT with scopes: repo + workflow
#   https://github.com/settings/tokens
GITHUB_TOKEN=
